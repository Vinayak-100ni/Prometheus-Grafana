1. create new dashboard
2. create an variable for uptime data for that
   > settings

       variables +add

       name        - job
       label       - job
       querie type - label value
        =run queries
        
4. inside queries select {code}
5. ```bash time() - process_start_time_seconds{job="$job"} ``` use this querie to get uptime
